# webserbisyo-red-oscar-landing

Standalone Next.js setup for the Red Oscar custom landing page.

This repo is marketing-only. Menu data, ordering, payments, and shared backend/CMS behavior stay in the main WebSerbisyo app.

## Local development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
